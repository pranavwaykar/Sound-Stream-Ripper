[phases.setup]
nixPkgs = ["nodejs_18", "ffmpeg"]

[phases.install]
cmds = [
  "npm install",
  "cd frontend && npm install", 
  "cd backend && npm install"
]

[phases.build]
cmds = [
  "cd frontend && npm run build"
]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production" 